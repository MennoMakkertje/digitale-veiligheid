const sideCheckbox = document.getElementById("tegel-active");

document.getElementById("INbn1").addEventListener("click", function() {
  sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.inleiding');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });

  document.getElementById("ODbn1").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.OD1');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });

  document.getElementById("ODbn2").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.OD2');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });

  document.getElementById("ODbn3").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.OD3');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });

  document.getElementById("ODbn4").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.OD4');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });

  document.getElementById("ODbn5").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.OD5');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });

  document.getElementById("ODbn6").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.OD6');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });

  document.getElementById("ODbn7").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie =  document.querySelector('.OD7');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  document.getElementById("INbn1A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.inleiding');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  
  document.getElementById("ODbn1A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.OD1');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  
  document.getElementById("ODbn2A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.OD2');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  
  document.getElementById("ODbn3A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.OD3');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  
  document.getElementById("ODbn4A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.OD4');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  
  document.getElementById("ODbn5A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.OD5');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  
  document.getElementById("ODbn6A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.OD6');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  
  document.getElementById("ODbn7A").addEventListener("click", function() {
    sideCheckbox.checked = false;
    // Bepaal het doel-element
    const doelSectie = document.querySelector('.OD7');
  
    // Bereken de positie van het element, minus een offset voor de sticky header (bijv. 80px)
    const offset = doelSectie.getBoundingClientRect().top + window.pageYOffset - 80;
  
    // Scroll naar de berekende positie
    window.scrollTo({
      top: offset,
      behavior: "smooth"
    });
  });
  